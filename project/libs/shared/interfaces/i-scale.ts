export interface IScale {
  id?: string;
  name: string;
  description?: string;
  // createdBy?: IUser;
  // answers?: ScaleAnswerEntity[];
  // criteria?: CriterionEntity[];
  // test?: TestEntity;
  // createdBy?: UserEntity;
}
